# 📋 后端功能补齐计划

**目标**：按 UI.pen 功能映射清单，补齐后端接口与模型，实现全量验证测试
**预计总耗时**：10-14 天
**启动时间**：2026-07-08

---

## 第一阶段：P0 闭环（2-3 天）
让现有 5 个页面从"有接口"变成"能跑通真实数据"

### 1.1 热榜接口扩展
- [ ] 扩展 `GET /topics` 返回情感联表数据（情感标签 + 置信度）
- [ ] 扩展 `GET /topics/{id}` 返回平台扩散、排名、样本
- [ ] 新增 `GET /topics/facets` 分类统计标签

### 1.2 统计接口扩展
- [ ] 扩展 `GET /stats/overview` 补充负面占比、昨日对比、下次采集时间
- [ ] 新增 `GET /stats/platform-distribution` 各平台分布
- [ ] 新增 `GET /stats/kpi-deltas` 日环比

### 1.3 爬虫接口扩展
- [ ] 扩展 `GET /crawler/status` 补充真实队列、进度、当前平台
- [ ] 新增 `GET /sync/status` 顶栏同步状态

### 1.4 统一响应格式
- [ ] 统一所有列表接口返回 `{ items, pagination }`
- [ ] 修正 `topics.py` 中未导入的 `HTTPException`

---

## 第二阶段：P1 业务功能（4-6 天）
支撑 UI.pen 中明显的一线业务功能

### 2.1 预警模块
- [ ] 创建 `alert_rules` 模型 + CRUD 接口
- [ ] 创建 `alert_events` 模型 + 查询接口
- [ ] 创建 `alert_actions` 模型 + 处置接口
- [ ] 新增 `GET /alerts` 预警队列
- [ ] 新增 `POST /alerts/{id}/ack` 确认
- [ ] 新增 `POST /alerts/{id}/resolve` 解决

### 2.2 平台监测
- [ ] 新增 `GET /platforms/monitoring` 六平台状态矩阵
- [ ] 新增 `GET /stats/platform-matrix` 平台对比
- [ ] 新增 `GET /data-quality/freshness` 数据新鲜度

### 2.3 数据质量
- [ ] 创建 `data_quality_runs` + `data_quality_issues` 模型
- [ ] 新增 `GET /data-quality/funnel` 处理漏斗
- [ ] 新增 `GET /data-quality/checks` 质量检查
- [ ] 新增 `GET /data-quality/issues` 待处理问题

### 2.4 系统管理
- [ ] 新增 `GET /system/health` 系统健康
- [ ] 创建 `system_logs` + `audit_logs` 模型
- [ ] 新增 `GET /system/logs` 系统日志
- [ ] 新增 `GET /audit-logs` 审计日志

---

## 第三阶段：P2 高级分析（4-5 天）
支撑聚类、预测、解释等高级能力

### 3.1 话题聚类
- [ ] 创建 `topic_clusters` + `topic_cluster_members` 模型
- [ ] 新增 `GET /topics/clusters` 聚类主题
- [ ] 新增 `GET /topics/{id}/related` 关联话题

### 3.2 传播路径
- [ ] 创建 `propagation_edges` 模型
- [ ] 新增 `GET /topics/{id}/propagation` 传播路径

### 3.3 趋势预测
- [ ] 新增 `POST /forecast/heat` 热度预测（统计方法占位）
- [ ] 新增 `GET /forecast/signals` 预测信号

### 3.4 模型解释
- [ ] 创建 `model_versions` + `sentiment_explanations` 模型
- [ ] 新增 `GET /models/current` 模型状态
- [ ] 新增 `POST /sentiment/explain` 词级贡献

---

## 第四阶段：全量验证测试（1-2 天）

### 4.1 接口测试
- [ ] 每个新增接口至少 1 个正常用例 + 1 个错误用例
- [ ] 测试分页、筛选、排序一致性
- [ ] 测试跨模块数据一致性（如热榜→情感→统计链路）

### 4.2 集成测试
- [ ] 端到端测试：爬虫采集 → 入库 → 情感分析 → 统计展示
- [ ] 压力测试：热榜列表大数据量分页性能
- [ ] 异常测试：爬虫失败 fallback、数据库断开恢复

### 4.3 文档更新
- [ ] 更新 `docs/API接口文档.md`，补充所有新增接口
- [ ] 更新 `docs/UI后端功能映射清单.md` 状态列
- [ ] 提交最终代码并推送

---

## 当前执行状态

| 阶段 | 进度 | 状态 |
|------|------|------|
| 第一阶段：P0 闭环 | 0/4 | ⏳ 待开始 |
| 第二阶段：P1 业务功能 | 0/4 | ⏳ 待开始 |
| 第三阶段：P2 高级分析 | 0/4 | ⏳ 待开始 |
| 第四阶段：全量验证测试 | 0/3 | ⏳ 待开始 |
