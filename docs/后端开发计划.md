# 后端功能补齐计划

> 更新时间：2026-07-08
> 目标：按 UI.pen 功能映射清单补齐后端接口与模型，并完成前后端契约验证。

## 执行状态

| 阶段 | 状态 | 说明 |
|------|------|------|
| 第一阶段：P0 闭环 | 已完成 | 热榜情感联表、统计扩展、爬虫状态、同步状态、分页契约已补齐 |
| 第二阶段：P1 业务功能 | 已完成 | 预警、平台监测、数据质量、系统日志、审计日志已补齐 |
| 第三阶段：P2 高级分析 | 已完成 | 聚类、传播、预测、模型解释、模型状态已补齐基线实现 |
| 第四阶段：全量验证测试 | 已完成 | 契约测试、前端构建、运行态接口、爬虫入库均已验证 |

## 已完成清单

### P0 闭环

- [x] 扩展 `GET /topics` 返回情感联表数据。
- [x] 扩展 `GET /topics/{id}` 返回平台、情感、原始字段。
- [x] 新增 `GET /topics/facets` 分类统计标签。
- [x] 扩展 `GET /stats/overview`，补充负面占比、昨日对比和采集摘要。
- [x] 新增 `GET /stats/platform-distribution`。
- [x] 新增 `GET /stats/kpi-deltas`。
- [x] 扩展 `GET /crawler/status`，补充真实队列、进度和平台状态。
- [x] 新增 `GET /sync/status` 顶栏同步状态。
- [x] 统一列表接口为 `{ items, pagination }`。
- [x] 修正动态路由冲突，保证 `topics/{id}`、`platforms/{id}` 与扩展路径共存。

### P1 业务功能

- [x] 补齐 `alert_rules`、`alert_events`、`alert_actions` 模型和接口。
- [x] 新增 `GET /alerts`、`GET /alerts/summary`、预警确认/解决/忽略接口。
- [x] 新增 `GET /platforms/monitoring`、`GET /stats/platform-matrix`、`GET /data-quality/freshness`。
- [x] 补齐数据质量漏斗、检查项、问题列表、手动运行接口。
- [x] 补齐 `system_logs`、`audit_logs` 及系统健康接口。
- [x] 补齐平台请求配置详情和配置变更审计。

### P2 高级分析

- [x] 补齐主题聚类列表、详情、运行接口。
- [x] 补齐话题样本、关联话题、传播路径、传播强度接口。
- [x] 新增 `POST /forecast/heat` 热度预测。
- [x] 新增 `GET /forecast/signals`、`GET /forecast/scenarios`。
- [x] 补齐模型状态、版本、指标和解释接口。
- [x] 补齐情感任务队列、任务详情和重试接口。
- [x] 新增非破坏式数据归档接口。

## 验证命令

```powershell
python -m compileall backend\app backend\tests crawler
pytest backend\tests -q
cd frontend; npm run build
```

## 当前验收结果

- 后端测试：45 passed。
- 前端生产构建：通过。
- 前端 API 封装与 OpenAPI 对比：57 个调用，0 个缺失。
- OpenAPI 注册：122 个 method operation。
- 爬虫入库：六个平台均可用，当前热点 419 条、情感结果 419 条。

## 后续可选优化

- 将当前统计/规则基线聚类替换为 sklearn 或向量聚类。
- 将当前移动平均热度预测替换为带回测的时序模型。
- 为通知服务接入生产 SMTP 通道。
